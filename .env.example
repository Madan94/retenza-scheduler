# Copy to .env and fill in values (root, backend/, and worker/ each need DATABASE_URL where used)

DATABASE_URL=postgresql://postgres:password@localhost:5432/retenza
REDIS_HOST=localhost
REDIS_PORT=6379

# Worker only
EMAIL_USER=
EMAIL_PASS=
TELEGRAM_BOT_TOKEN=
